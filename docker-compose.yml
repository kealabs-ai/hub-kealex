services:
  svc-auth:
    image: kealex/svc-auth:latest
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - DATABASE_URL=${DATABASE_URL}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.kealex-auth.rule=Host(`srv1023256.hstgr.cloud`) && PathPrefix(`/v1/lex/auth`)"
      - "traefik.http.routers.kealex-auth.entrypoints=https"
      - "traefik.http.routers.kealex-auth.tls=true"
      - "traefik.http.middlewares.kealex-strip-auth.stripprefix.prefixes=/v1/lex"
      - "traefik.http.middlewares.kealex-cors.headers.accesscontrolalloworiginlist=https://srv1023256.hstgr.cloud,http://localhost:5173,http://localhost:3000"
      - "traefik.http.middlewares.kealex-cors.headers.accesscontrolallowmethods=GET,POST,PUT,DELETE,OPTIONS"
      - "traefik.http.middlewares.kealex-cors.headers.accesscontrolallowheaders=Content-Type,Authorization,Accept,X-Requested-With"
      - "traefik.http.middlewares.kealex-cors.headers.accesscontrolmaxage=86400"
      - "traefik.http.middlewares.kealex-cors.headers.addvaryheader=true"
      - "traefik.http.middlewares.kealex-cors.headers.accesscontrolallowcredentials=true"
      - "traefik.http.routers.kealex-auth.middlewares=kealex-strip-auth,kealex-cors"
      - "traefik.http.services.kealex-auth.loadbalancer.server.port=8000"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - easypanel

  svc-processos:
    image: kealex/svc-processos:latest
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - DATABASE_URL=${DATABASE_URL}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.kealex-processos.rule=Host(`srv1023256.hstgr.cloud`) && PathPrefix(`/v1/lex/processos`)"
      - "traefik.http.routers.kealex-processos.entrypoints=https"
      - "traefik.http.routers.kealex-processos.tls=true"
      - "traefik.http.routers.kealex-processos.middlewares=kealex-strip-auth,kealex-cors"
      - "traefik.http.services.kealex-processos.loadbalancer.server.port=8000"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - easypanel

  svc-documentos:
    image: kealex/svc-documentos:latest
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - DATABASE_URL=${DATABASE_URL}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.kealex-documentos.rule=Host(`srv1023256.hstgr.cloud`) && PathPrefix(`/v1/lex/documentos`)"
      - "traefik.http.routers.kealex-documentos.entrypoints=https"
      - "traefik.http.routers.kealex-documentos.tls=true"
      - "traefik.http.routers.kealex-documentos.middlewares=kealex-strip-auth,kealex-cors"
      - "traefik.http.services.kealex-documentos.loadbalancer.server.port=8000"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - easypanel

  svc-financeiro:
    image: kealex/svc-financeiro:latest
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - DATABASE_URL=${DATABASE_URL}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.kealex-financeiro.rule=Host(`srv1023256.hstgr.cloud`) && PathPrefix(`/v1/lex/financeiro`)"
      - "traefik.http.routers.kealex-financeiro.entrypoints=https"
      - "traefik.http.routers.kealex-financeiro.tls=true"
      - "traefik.http.routers.kealex-financeiro.middlewares=kealex-strip-auth,kealex-cors"
      - "traefik.http.services.kealex-financeiro.loadbalancer.server.port=8000"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - easypanel

  svc-prazos:
    image: kealex/svc-prazos:latest
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - DATABASE_URL=${DATABASE_URL}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.kealex-prazos.rule=Host(`srv1023256.hstgr.cloud`) && PathPrefix(`/v1/lex/prazos`)"
      - "traefik.http.routers.kealex-prazos.entrypoints=https"
      - "traefik.http.routers.kealex-prazos.tls=true"
      - "traefik.http.routers.kealex-prazos.middlewares=kealex-strip-auth,kealex-cors"
      - "traefik.http.services.kealex-prazos.loadbalancer.server.port=8000"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - easypanel

  svc-usuarios:
    image: kealex/svc-usuarios:latest
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - DATABASE_URL=${DATABASE_URL}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.kealex-usuarios.rule=Host(`srv1023256.hstgr.cloud`) && PathPrefix(`/v1/lex/usuarios`)"
      - "traefik.http.routers.kealex-usuarios.entrypoints=https"
      - "traefik.http.routers.kealex-usuarios.tls=true"
      - "traefik.http.routers.kealex-usuarios.middlewares=kealex-strip-auth,kealex-cors"
      - "traefik.http.services.kealex-usuarios.loadbalancer.server.port=8000"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - easypanel

  svc-configuracoes:
    image: kealex/svc-configuracoes:latest
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - DATABASE_URL=${DATABASE_URL}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.kealex-configuracoes.rule=Host(`srv1023256.hstgr.cloud`) && PathPrefix(`/v1/lex/configuracoes`)"
      - "traefik.http.routers.kealex-configuracoes.entrypoints=https"
      - "traefik.http.routers.kealex-configuracoes.tls=true"
      - "traefik.http.routers.kealex-configuracoes.middlewares=kealex-strip-auth,kealex-cors"
      - "traefik.http.services.kealex-configuracoes.loadbalancer.server.port=8000"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - easypanel

  svc-escritorios:
    image: kealex/svc-escritorios:latest
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - DATABASE_URL=${DATABASE_URL}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.kealex-escritorios.rule=Host(`srv1023256.hstgr.cloud`) && PathPrefix(`/v1/lex/escritorios`)"
      - "traefik.http.routers.kealex-escritorios.entrypoints=https"
      - "traefik.http.routers.kealex-escritorios.tls=true"
      - "traefik.http.routers.kealex-escritorios.middlewares=kealex-strip-auth,kealex-cors"
      - "traefik.http.services.kealex-escritorios.loadbalancer.server.port=8000"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - easypanel

  svc-clientes:
    image: kealex/svc-clientes:latest
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - DATABASE_URL=${DATABASE_URL}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.kealex-clientes.rule=Host(`srv1023256.hstgr.cloud`) && PathPrefix(`/v1/lex/clientes`)"
      - "traefik.http.routers.kealex-clientes.entrypoints=https"
      - "traefik.http.routers.kealex-clientes.tls=true"
      - "traefik.http.routers.kealex-clientes.middlewares=kealex-strip-auth,kealex-cors"
      - "traefik.http.services.kealex-clientes.loadbalancer.server.port=8000"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - easypanel

networks:
  easypanel:
    external: true