# 🔄 FLUXO COMPLETO DE INTEGRAÇÃO - PROCESSOS COM FASES\n\n## 1. Usuário Cria um Novo Processo\n\n### Frontend (ProcessosPage.tsx)\n```typescript\nconst openCreate = () => { \n  reset({}); \n  setEditing(null); \n  setOpen(true) \n}\n\nconst onSubmit = (data: FormData) => {\n  create.mutate(data as any, { onSuccess: close })\n}\n```\n\n### API Call (processos.ts)\n```typescript\nPOST /k1/lex/processos\n{\n  \"numero\": \"0001234-56.2024.8.26.0100\",\n  \"titulo\": \"Ação Civil\",\n  \"descricao\": \"...\",\n  \"clienteId\": \"cliente-uuid\",\n  \"vara\": \"1ª Vara Cível\",\n  \"tribunal\": \"TJSP\"\n}\n```\n\n### Backend Response (svc-processos/main.py)\n```python\n# Criar processo\np = Processo(...)\ndb.add(p)\ndb.commit()\ndb.refresh(p)\n\n# Criar fases padrão\nfases_padrao = [\n    Fase(processo_id=p.id, label=\"Protocolo\", ordem=0, status=\"ativa\"),\n    Fase(processo_id=p.id, label=\"Citação\", ordem=1, status=\"futura\"),\n    Fase(processo_id=p.id, label=\"Contestação\", ordem=2, status=\"futura\"),\n    Fase(processo_id=p.id, label=\"Sentença\", ordem=3, status=\"futura\"),\n    Fase(processo_id=p.id, label=\"Recurso\", ordem=4, status=\"futura\"),\n    Fase(processo_id=p.id, label=\"Encerrado\", ordem=5, status=\"futura\"),\n]\ndb.add_all(fases_padrao)\ndb.commit()\n\n# Retornar com fases\nreturn _to_dict(p, cliente)\n```\n\n### Response\n```json\n{\n  \"id\": \"processo-uuid\",\n  \"numero\": \"0001234-56.2024.8.26.0100\",\n  \"titulo\": \"Ação Civil\",\n  \"faseAtual\": 0,\n  \"fases\": [\n    {\n      \"id\": \"fase-uuid-1\",\n      \"label\": \"Protocolo\",\n      \"status\": \"ativa\",\n      \"data\": null\n    },\n    {\n      \"id\": \"fase-uuid-2\",\n      \"label\": \"Citação\",\n      \"status\": \"futura\",\n      \"data\": null\n    },\n    ...\n  ]\n}\n```\n\n### Frontend Update\n```typescript\n// Hook invalida cache\nuseCreateProcesso() {\n  return useMutation({\n    mutationFn: processosApi.create,\n    onSuccess: () => qc.invalidateQueries({ queryKey: PROCESSOS_KEY })\n  })\n}\n\n// useProcessos refetch automático\nuseProcessos() {\n  return useQuery({ \n    queryKey: PROCESSOS_KEY, \n    queryFn: processosApi.list\n  })\n}\n```\n\n---\n\n## 2. Usuário Visualiza Processos\n\n### Frontend (ProcessosPage.tsx)\n```typescript\nconst { data: processos, isLoading } = useProcessos()\n\n// Renderizar lista\n{displayProcessos.map((p) => (\n  <div key={p.id}>\n    <span>{p.numero}</span>\n    <p>{p.titulo}</p>\n    {/* Timeline expandível */}\n  </div>\n))}\n```\n\n### API Call\n```typescript\nGET /k1/lex/processos\n```\n\n### Backend Response\n```python\n@app.get(\"/k1/lex/processos\")\ndef list_processos(db: Session = Depends(get_db), payload=Depends(verify_token)):\n    role, uid, tid = payload.get(\"role\"), payload.get(\"sub\"), payload.get(\"tenant_id\")\n    q = db.query(Processo).filter_by(tenant_id=tid)\n    \n    if role == \"cliente\":\n        q = q.filter_by(cliente_id=uid)\n    elif role == \"advogado\":\n        q = q.filter_by(advogado_id=uid)\n    \n    return _enrich(db, q.all())\n```\n\n### Response\n```json\n[\n  {\n    \"id\": \"processo-uuid\",\n    \"numero\": \"0001234-56.2024.8.26.0100\",\n    \"titulo\": \"Ação Civil\",\n    \"faseAtual\": 0,\n    \"fases\": [...]\n  }\n]\n```\n\n---\n\n## 3. Usuário Expande Timeline\n\n### Frontend (ProcessosPage.tsx)\n```typescript\nconst [expandedTimeline, setExpandedTimeline] = useState<string | null>(null)\n\n<motion.button\n  onClick={() => setExpandedTimeline(isExpanded ? null : p.id)}\n  className=\"...\"\n>\n  <ChevronUp size={20} className={`transition-transform ${isExpanded ? 'rotate-180' : ''}`} />\n</motion.button>\n\n{isExpanded && (\n  <ProcessoTimeline\n    fases={p.fases}\n    faseAtual={p.faseAtual}\n    readonly={isCliente}\n    onAvancar={(novaFase) => {\n      avancarFase.mutate({ id: p.id, novaFase })\n    }}\n  />\n)}\n```\n\n### Componente (ProcessoTimeline.tsx)\n```typescript\nexport function ProcessoTimeline({ fases, faseAtual = 0, onAvancar, readonly }) {\n  const items = fases ?? DEFAULT_FASES.map((f, i) => ({\n    ...f,\n    id: String(i),\n    status: i < faseAtual ? 'concluida' : i === faseAtual ? 'ativa' : 'futura',\n  }))\n\n  const podeAvancar = !readonly && faseAtual < items.length - 1\n\n  return (\n    <div className=\"space-y-3\">\n      {/* Timeline visual */}\n      {items.map((fase, i) => (\n        <div key={fase.id}>\n          {/* Node com status visual */}\n          {/* Connector */}\n        </div>\n      ))}\n      \n      {podeAvancar && (\n        <button onClick={() => onAvancar?.(faseAtual + 1)}>\n          Avançar Fase\n        </button>\n      )}\n    </div>\n  )\n}\n```\n\n---\n\n## 4. Usuário Clica em \"Avançar Fase\"\n\n### Frontend (ProcessoTimeline.tsx)\n```typescript\n<motion.button\n  onClick={() => onAvancar?.(faseAtual + 1)}\n  className=\"flex items-center gap-1.5 px-3 py-1.5 bg-indigo-600 ...\"\n>\n  Avançar Fase <ChevronRight size={13} />\n</motion.button>\n```\n\n### Frontend (ProcessosPage.tsx)\n```typescript\nonAvancar={(novaFase) => {\n  avancarFase.mutate({ id: p.id, novaFase })\n}}\n```\n\n### Hook (useProcessos.ts)\n```typescript\nexport function useAvancarFase() {\n  const qc = useQueryClient()\n  return useMutation({\n    mutationFn: ({ id, novaFase }) => processosApi.avancarFase(id, novaFase),\n    onSuccess: () => qc.invalidateQueries({ queryKey: PROCESSOS_KEY })\n  })\n}\n```\n\n### API Call (processos.ts)\n```typescript\navancarFase: (id: string, novaFase: number) =>\n  api.post<Processo>('/k1/lex/processos/avancar-fase', { id, novaFase })\n    .then((r) => r.data)\n```\n\n### Backend Endpoint (svc-processos/main.py)\n```python\n@app.post(\"/k1/lex/processos/avancar-fase\")\ndef avancar_fase(body: AvancarFaseIn, db: Session = Depends(get_db), payload=Depends(verify_token)):\n    tenant_id = payload.get(\"tenant_id\")\n    p = db.query(Processo).filter_by(id=body.id, tenant_id=tenant_id).first()\n    if not p:\n        raise HTTPException(404, \"Processo não encontrado\")\n    \n    fases = sorted(p.fases, key=lambda f: f.ordem)\n    if body.novaFase < 0 or body.novaFase >= len(fases):\n        raise HTTPException(400, \"Fase inválida\")\n    \n    # Atualizar status das fases\n    for i, fase in enumerate(fases):\n        if i < body.novaFase:\n            fase.status = \"concluida\"\n            if not fase.data_conclusao:\n                fase.data_conclusao = datetime.utcnow()\n        elif i == body.novaFase:\n            fase.status = \"ativa\"\n        else:\n            fase.status = \"futura\"\n    \n    p.fase_atual = body.novaFase\n    p.updated_at = datetime.utcnow()\n    db.commit()\n    db.refresh(p)\n    cliente = db.query(Cliente).filter_by(id=p.cliente_id).first()\n    return _to_dict(p, cliente)\n```\n\n### Response\n```json\n{\n  \"id\": \"processo-uuid\",\n  \"numero\": \"0001234-56.2024.8.26.0100\",\n  \"titulo\": \"Ação Civil\",\n  \"faseAtual\": 1,\n  \"fases\": [\n    {\n      \"id\": \"fase-uuid-1\",\n      \"label\": \"Protocolo\",\n      \"status\": \"concluida\",\n      \"data\": \"2024-01-15T10:35:00\"\n    },\n    {\n      \"id\": \"fase-uuid-2\",\n      \"label\": \"Citação\",\n      \"status\": \"ativa\",\n      \"data\": null\n    },\n    {\n      \"id\": \"fase-uuid-3\",\n      \"label\": \"Contestação\",\n      \"status\": \"futura\",\n      \"data\": null\n    },\n    ...\n  ]\n}\n```\n\n### Frontend Update\n```typescript\n// Hook invalida cache\nuseAvancarFase() {\n  return useMutation({\n    mutationFn: ...,\n    onSuccess: () => qc.invalidateQueries({ queryKey: PROCESSOS_KEY })\n  })\n}\n\n// useProcessos refetch automático\n// Timeline renderiza com novos dados\n```\n\n---\n\n## 5. Timeline Atualiza Visualmente\n\n### Antes\n```\n✓ Protocolo (concluida)\n● Citação (ativa)\n○ Contestação (futura)\n○ Sentença (futura)\n○ Recurso (futura)\n○ Encerrado (futura)\n```\n\n### Depois\n```\n✓ Protocolo (concluida)\n✓ Citação (concluida)\n● Contestação (ativa)\n○ Sentença (futura)\n○ Recurso (futura)\n○ Encerrado (futura)\n```\n\n---\n\n## 📊 Diagrama de Fluxo\n\n```\nProcessosPage\n    ↓\n[Novo Processo]\n    ↓\nModal Form\n    ↓\nuseMutation (create)\n    ↓\nPOST /k1/lex/processos\n    ↓\nBackend: Criar Processo + 6 Fases\n    ↓\nResponse com fases\n    ↓\nInvalidar Cache\n    ↓\nuseProcessos refetch\n    ↓\nProcessosPage renderiza com fases\n    ↓\n[Expandir Timeline]\n    ↓\nProcessoTimeline renderiza\n    ↓\n[Avançar Fase]\n    ↓\nuseAvancarFase mutation\n    ↓\nPOST /k1/lex/processos/avancar-fase\n    ↓\nBackend: Atualizar fases\n    ↓\nResponse com fases atualizadas\n    ↓\nInvalidar Cache\n    ↓\nuseProcessos refetch\n    ↓\nTimeline renderiza com novo status\n```\n\n---\n\n## 🔄 Ciclo de Vida Completo\n\n1. **Criar** → Processo com 6 fases padrão\n2. **Listar** → Todos os processos com fases\n3. **Visualizar** → Timeline expandível\n4. **Avançar** → Próxima fase\n5. **Atualizar** → Status visual em tempo real\n6. **Repetir** → Até fase final\n\n---\n\n**Status: ✅ FLUXO COMPLETO IMPLEMENTADO E TESTADO**\n